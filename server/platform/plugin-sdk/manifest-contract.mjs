const ID=/^[a-z0-9]+(?:-[a-z0-9]+)*$/,VERSION=/^\d+\.\d+\.\d+(?:-[0-9A-Za-z.-]+)?$/;
export const PLUGIN_KINDS=Object.freeze(['tool','collector']);export const PLUGIN_RISK_LEVELS=Object.freeze(['read-only','local-write','network-read','browser-read','external-write']);
export function validatePluginManifestBase(manifest){const issues=[],add=(field,message)=>issues.push({field,level:'error',message});if(!manifest||typeof manifest!=='object'||Array.isArray(manifest))return [{field:'manifest',level:'error',message:'Plugin Manifest 必须是对象'}];if(manifest.schemaVersion!==1)add('schemaVersion','schemaVersion 必须为 1');if(!ID.test(manifest.id||''))add('id','插件 ID 必须使用 kebab-case');if(typeof manifest.name!=='string'||!manifest.name.trim())add('name','插件名称不能为空');if(!VERSION.test(manifest.version||''))add('version','插件版本必须使用 SemVer');if(!PLUGIN_KINDS.includes(manifest.kind))add('kind','kind 必须为 tool 或 collector');if((typeof manifest.entry!=='string'||!manifest.entry.trim())&&!manifest.endpoint)add('entry','本地插件 entry 不能为空');if(!Array.isArray(manifest.capabilities)||!manifest.capabilities.length||manifest.capabilities.some((item)=>typeof item!=='string'||!item.trim())||new Set(manifest.capabilities).size!==manifest.capabilities.length)add('capabilities','capabilities 必须是非空且不重复的字符串数组');if(!PLUGIN_RISK_LEVELS.includes(manifest.riskLevel))add('riskLevel','riskLevel 无效');if(manifest.inputSchema?.type!=='object')add('inputSchema','inputSchema 必须是 object Schema');if(manifest.outputSchema?.type!=='object')add('outputSchema','outputSchema 必须是 object Schema');if(!manifest.runtime||typeof manifest.runtime!=='object'||Array.isArray(manifest.runtime))add('runtime','runtime 必须是对象');return issues;}
