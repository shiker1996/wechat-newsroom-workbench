# AI_DESIGN_SPEC 模板

```markdown
# {label} · Social AI Design Spec

## 主题定位

（说明主题视觉气质和识别关键词；主题识别应在原尺寸下可感知，但具体页面载体和构图由 AI 视觉生成技能决定。）

## 设计 Token

### 配色

### 字体

### 形状与效果

（只说明字体家族和主题阴影/装饰语言。排版和布局由 AI 视觉生成技能的通用参考负责。）

## 组件视觉绑定

（声明主题类名前缀，以及颜色、字体、边框、圆角、投影、渐变和装饰如何覆盖通用组件。保留通用页面骨架、组件语义和 HTML 层级；允许自由设计组件内部构图。）

## 语义组件配方

（提供 `metric-focus`、`process-rail`、`signal-grid`、`warning-panel`、`terminal-panel`、`accent-fill` 等语义组件在本主题下适合采用的视觉处理。它们是可组合的视觉候选，不是固定页面模板。）

## 主题视觉应用

（说明本主题的颜色、字体、边框、圆角、投影、渐变和装饰如何覆盖通用组件，并明确 `accent / accent2 / surface / inverse` 如何形成层级对比。页面角色、内容层组合、页面数量、结构和版式由 AI 视觉生成技能定义。）

每个主题 SPEC 都应填入实际 Token、主题前缀类名、主题配方和适用场景；模板本身不是最终设计规范。
