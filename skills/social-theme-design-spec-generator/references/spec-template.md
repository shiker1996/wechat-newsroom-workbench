# AI_DESIGN_SPEC 模板

```markdown
# {label} · Social AI Design Spec

## 主题定位

（说明主题视觉气质，并明确：每一页都要能一眼看到主题的视觉语言，封面和内容页同样要求高冲击力，不允许内容页比封面平淡。）

## 设计 Token

### 配色

### 字体与间距

### 形状与效果

（必须包含主题投影写法：hard 用硬投影 / soft 用柔和投影 / glow 用霓虹光晕 / none 用边框与色块分层；并给出低透明度 radial-gradient 氛围写法。）

## 页面结构边界

（只说明本主题如何套用通用页面骨架；完整结构、内容层数量、居中方式和画布规则由 AI 视觉生成技能的通用契约与 Layout Guide 定义，不在此重复。）

## 组件视觉绑定

（声明主题类名前缀，以及颜色、字体、边框、圆角、投影、渐变和装饰如何覆盖通用组件。组件语义、HTML 层级和基础 CSS 引用 `xhs-visual-contract.md`，不在主题 SPEC 中复制一套组件目录。）

## 主题视觉应用

（说明本主题的颜色、字体、边框、圆角、投影、渐变和装饰如何覆盖通用组件，并明确 `accent / accent2 / surface / inverse` 如何形成强、中、弱三档层级，以及如何表现数字焦点、证据边框卡和结论色块。页面角色、内容层组合、页面数量和结构由 AI 视觉生成技能的通用契约定义。）

## 通用门禁引用

（声明画布、安全区、字号、利用率、溢出、裁切、内部滚动和事实完整性由 AI 视觉技能、Layout Guide 与通用视觉契约统一定义。）

## 最小示例
```

每个主题 SPEC 都应填入实际 Token、主题前缀类名、主题配方和适用场景；模板本身不是最终设计规范。
