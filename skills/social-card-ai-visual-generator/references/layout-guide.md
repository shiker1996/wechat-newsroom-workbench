# 小红书图文排版规范 v3

---

## 📐 页面规格

| 项目 | 规范 |
|------|------|
| 设计尺寸 | 375 × 667px |
| PNG 缩放 | 由截图阶段决定，不作为 HTML 生成约束 |
| 页面比例 | 9:16 |
| 安全区域 | 左右 ≥16px，上下 ≥28px |

---

## 🧱 布局结构

```html
<section class="page">
  <div class="page-inner">
    <header class="neon-topbar"> <!-- 顶部栏 --> </header>
    <div class="page-body">       <!-- 主内容区 --> </div>
    <div class="bottom-strip">    <!-- 底部导航 --> </div>
  </div>
</section>
```

```css
*, *::before, *::after {
  box-sizing: border-box;
}
.page {
  width: 375px;
  height: 667px;
  /* 仅用于固定画布和背景装饰的边界；不能用来隐藏内容 */
  overflow: hidden;
  position: relative;
}
.page-inner {
  height: 100%;
  padding: 0 16px 32px;
  display: flex;
  flex-direction: column;
}
.page-body {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 8px;
}
```

---

## 🔤 字体规范

以下是 AI 视觉页面的字号范围参考；主题 SPEC 可在保持可读性和布局门禁的前提下调整字体风格与档位：

| 类型 | 字号 | 字重 |
|------|------|------|
| 封面标题 | 28-40px | 800-900 |
| 页面标题 | 14-18px | 700-800 |
| 卡片标题 | 13-16px | 700-800 |
| 正文 | 11-15px | 400-500 |
| 辅助文字 | 10-12px | 400 |
| 标签 | 10-11px | 600-700 |

行高：标题 1.2-1.4，正文 1.5-1.7

---

## 📏 间距系统（8px 基准）

| 类型 | 数值 |
|------|------|
| 超小间距 | 4px |
| 小间距 | 8px |
| 中间距 | 12px |
| 标准间距 | 16px |
| 大间距 | 24px |

---

## 🎨 页面利用率与审计口径

| 利用率 | 判定 |
|--------|------|
| 82%-95% | ✅ 合理 |
| 95%+ | ⚠️ 风险较高 |
| 100%+ | ❌ 溢出 |

上表是视觉设计的推荐目标，不是唯一的通过条件。交付审计按真实 `.page-body` 可用区域和可见内容边界计算，并使用以下硬门槛：

| 页面类型 | 审计硬区间 |
|----------|------------|
| `cover` | 45%-90% |
| `content` | 50%-96% |
| `ending` | 20%-90% |

低于硬区间会标记 `underfilled`，高于硬区间会标记 `overfilled`；溢出、裁切、水平越界、文字过小、文字不可见和垂直失衡仍会直接阻断交付。不要为了追求推荐利用率添加空白卡、重复文案或无意义装饰。

最终利用率、溢出、裁切和垂直平衡以 Pipeline 的浏览器审计报告为准；本指南不要求 Agent 自行执行或模拟审计。

封面审计以 `.cover-center` 和 `.cover-bottom` 的实际可见子元素为内容边界，不把 `cover-center` 的 `flex:1` 全高当成已用内容；顶部角标属于装饰层，不单独撑高利用率。

---

## 📱 适配要点

- **顶部安全区**：避免被小红书 UI 遮挡，建议 padding-top: 36px（非 page-cover 页面）
- **底部安全区**：避免被点赞/评论栏遮挡，建议 padding-bottom: 42px
- **整体垂直居中**：封面、内容页和结尾页的内容栈统一居中，卡片内部文字仍按语义默认左对齐

---

## 🧠 内容排版原则

1. **一页只讲一个重点**，不要一页塞完整篇文章
2. **单段不超过 4 行**，保持呼吸感
3. **内容过多时优先拆页**，不压缩字体/行高
4. **留白是视觉缓冲，不是浪费**
5. **cover / 金句页**：留白 30%-45%，大字
6. **内容页**：建议按真实内容保持约 5%-18% 呼吸空间，对应 82%-95% 的推荐利用率；不要用空白卡或无意义装饰填充
7. **辅助文字和标签最小不低于 10px**，正文最小 ≥11px；这与浏览器审计规则一致
8. **整组垂直对齐统一**：封面、普通内容页和结尾页的 `.page-body` 均整体垂直居中；卡片内部文字仍默认左对齐
9. **每页一个首要焦点**：数字、证据、人物、步骤或结论中只选择最符合本页职责的一种作为最大视觉层，其他信息降为次级层
10. **相邻页面保持节奏变化**：主组件类型、强调位置、卡片轮廓或明暗层级至少有一项不同；事实结构相同且重复最合理时不强制变化
11. **主题化强调**：数字焦点、证据卡和结论色块使用当前主题的 `accent / accent2 / surface / inverse`，不固定蓝橙色，也不靠无意义 Emoji 制造变化

---

*版本：v3.0*
