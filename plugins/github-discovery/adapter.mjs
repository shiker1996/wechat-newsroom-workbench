import path from 'node:path';import { discoverGitHubRepositories } from './collector.mjs';import { ok } from './result.mjs';
export function createAdapter({config,onProgress=()=>{},githubQueries=[],configuration={},github:githubService}={}){const github={...config.githubDiscovery,...configuration};return {collect:async(source)=>ok(await discoverGitHubRepositories([],{...github,...source,requestGitHubJson:githubService?.requestGitHubJson,searchEnabled:github.enabled,aiQueries:githubQueries,cacheDir:path.join(config.workspaceRoot,'data','github-cache')},onProgress),{fetchMethod:'github-api'})};}
