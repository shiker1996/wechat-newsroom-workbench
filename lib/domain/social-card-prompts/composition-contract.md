## 固定构图字段要求

card_plan 每页增加 role（cover|concept|feature|steps|data|compare|evidence|timeline|risk|ending）。可选 composition 只允许由系统规范化，字段为 id、columns、flow、alignment、decoration、overlap；decoration 仅可表达 none/orbit/index-line/stamp，overlap 仅可表达 none/title-card/accent-edge。不要输出 CSS、坐标、尺寸或 HTML。
