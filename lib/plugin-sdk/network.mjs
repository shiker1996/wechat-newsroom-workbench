import net from 'node:net';
export function privateIp(address){if(net.isIPv4(address)){const [a,b]=address.split('.').map(Number);return a===10||a===127||a===0||(a===100&&b>=64&&b<=127)||(a===169&&b===254)||(a===172&&b>=16&&b<=31)||(a===192&&[0,2,168].includes(b))||(a===198&&(b===18||b===19||b===51))||(a===203&&b===0)||a>=224;}const value=String(address||'').toLowerCase().split('%')[0];if(!net.isIPv6(value))return true;if(value.startsWith('::ffff:')){const mapped=value.slice(7);return net.isIPv4(mapped)?privateIp(mapped):true;}if(value.startsWith('2002:')){const parts=value.split(':');if(!/^[0-9a-f]{4}$/.test(parts[1]||'')||!/^[0-9a-f]{4}$/.test(parts[2]||''))return true;const hex=`${parts[1]}${parts[2]}`;return privateIp([0,2,4,6].map((offset)=>parseInt(hex.slice(offset,offset+2),16)).join('.'));}return !/^[23][0-9a-f]{0,3}:/.test(value)||value.startsWith('2001:db8:')||value.startsWith('2001:10:')||value.startsWith('2001:2:')||value.startsWith('2001:0:');}
