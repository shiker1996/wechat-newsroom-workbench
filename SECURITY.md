# 安全政策

## 支持版本

本项目处于 0.x 阶段，仅最新发布系列（当前为 `0.5.x`）会获得安全修复；更早的 minor 系列不再维护。当前版本以 `package.json` 为唯一来源。

## 适用边界

本项目是本机单用户工具，HTTP 服务固定绑定 `127.0.0.1`，设计上不面向公网或局域网部署。以下情况**不属于**安全漏洞：

- 因用户自行修改代码把服务暴露到非回环地址而导致的问题；
- 攻击者已经能在本机以当前用户身份执行任意代码的场景（此时本地工具无法提供额外防护）；
- `x-admin-confirm` / `x-restore-confirm` 确认头可被绕过——它们是防误操作确认，不是鉴权，详见 README「安全边界」。

以下情况**属于**我们希望收到的报告：

- 路径穿越导致读取或写入工作区授权范围之外的文件；
- 备份恢复、插件安装、远程 adapter 等环节可执行非预期代码或覆盖非预期文件；
- URL 抓取 / 远程插件绕过 SSRF 防护访问内网地址；
- HTML / Markdown 预览在浏览器中执行注入脚本或泄露本地文件；
- 凭据（API Key、Cookie、Token）被写入日志、产物、快照或仓库文件；
- 未经用户逐次确认即发生外部写入（CDN 上传等）。

## 报告渠道

请**不要**在公开 Issue 中提交安全漏洞细节。请通过 GitHub 的私有漏洞报告（Private vulnerability reporting / Security Advisories）提交；在仓库公开前，可先通过 Issue 只描述"存在一个待私下沟通的安全问题"并留下联系方式，维护者会主动联系。

报告请尽量包含：影响范围、复现步骤、相关路由或文件路径、是否涉及真实凭据。**不要**在报告中附带真实密钥、用户数据或未脱敏的日志。

## 响应预期

- 收到报告后 7 天内确认是否受理；
- 确认受理后，高危问题优先在下一个补丁版本修复，修复前可与报告者协商临时缓解措施；
- 修复发布后在 CHANGELOG 中致谢（可匿名）。

## 用户侧安全建议

- 不要把 `.env`、`config.local.json`、`account-context.json` 提交到任何仓库或发送给他人；
- 轮换任何曾经泄露过的密钥后再继续使用；
- 安装第三方技能包 / 工具插件前审阅其代码与 manifest，安装行为本身即视为信任该包；
- 恢复备份只使用自己导出的 ZIP，不要恢复来源不明的备份包。
